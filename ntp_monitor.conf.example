# NTP Monitor 설정 파일
# 이 파일을 ~/.ntp_monitor.conf 또는 /etc/ntp_monitor.conf 에 복사하세요

[monitoring]
# NTP 지터 임계치 (초 단위)
jitter_threshold = 2.0

# 디버그 모드 (true/false)
debug_mode = true

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
log_level = INFO

[logging]
# Syslog 주소 (Linux: /dev/log, macOS/네트워크: localhost:514)
syslog_address = localhost:514
